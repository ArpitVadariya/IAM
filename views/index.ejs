<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Create User</title>
    <script src="https://cdn.tailwindcss.com"></script>
  </head>
  <body
    class="bg-zinc-900 text-white flex items-center justify-center min-h-screen"
  >
    <div class="bg-zinc-800 p-10 rounded-2xl shadow-2xl w-full max-w-md">
      <h1 class="text-4xl font-bold mb-8 text-center text-blue-400">
        Create User
      </h1>
      <form action="/create" method="post" class="space-y-5">
        <div>
          <label class="block mb-1 text-sm font-medium">Username</label>
          <input
            class="w-full px-4 py-2 rounded-lg bg-zinc-700 border border-zinc-600 focus:outline-none focus:ring-2 focus:ring-blue-400"
            type="text"
            name="username"
            placeholder="Enter username"
            required
          />
        </div>
        <div>
          <label class="block mb-1 text-sm font-medium">Email</label>
          <input
            class="w-full px-4 py-2 rounded-lg bg-zinc-700 border border-zinc-600 focus:outline-none focus:ring-2 focus:ring-blue-400"
            type="email"
            name="email"
            placeholder="Enter email"
            required
          />
        </div>
        <div>
          <label class="block mb-1 text-sm font-medium">Password</label>
          <input
            class="w-full px-4 py-2 rounded-lg bg-zinc-700 border border-zinc-600 focus:outline-none focus:ring-2 focus:ring-blue-400"
            type="password"
            name="password"
            placeholder="Enter password"
            required
          />
        </div>
        <div>
          <label class="block mb-1 text-sm font-medium">Age</label>
          <input
            class="w-full px-4 py-2 rounded-lg bg-zinc-700 border border-zinc-600 focus:outline-none focus:ring-2 focus:ring-blue-400"
            type="number"
            name="age"
            placeholder="Enter age"
            required
          />
        </div>
        <div class="text-center">
          <input
            class="px-6 py-2 bg-blue-500 hover:bg-blue-600 transition-colors duration-200 text-white font-semibold rounded-lg cursor-pointer"
            type="submit"
            value="Create User"
          />
        </div>
      </form>
    </div>
  </body>
</html>
